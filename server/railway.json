{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-package-lock && npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}