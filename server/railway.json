{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-package-lock && npx prisma generate && npx prisma db push && npx tsx src/seed.ts"
  },
  "deploy": {
    "startCommand": "npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}